[project]
name = "codedrills-content-review"
version = "0.1.0"
description = "Local editor and review workflow for CodeDrills content"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.40",
]

[tool.uv]
package = false